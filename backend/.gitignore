node_modules
# Keep environment variables out of version control
.env

/generated/prisma
/uploads

# Build/test artifacts
/dist
/coverage
# Stray .js next to .ts in test/ — only sources should be tracked
test/*.js

